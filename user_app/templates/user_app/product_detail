<!DOCTYPE html>
  <html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SmashStrix - Product Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/zoom-vanilla.js/1.0.0/zoom-vanilla.min.js"></script>
    <style>
      .zoom-img {
        cursor: zoom-in;
      }
      .zoom-img.zoomed {
        cursor: zoom-out;
      }
    </style>
  </head>
  <body class="bg-gray-50 font-sans">
    <nav class="bg-blue-800 text-white p-4 shadow-md">
      <div class="container mx-auto flex justify-between items-center">
        <h1 class="text-2xl font-bold">SmashStrix</h1>
        <a href="/" class="hover:underline">Home</a>
      </div>
    </nav>
    <div class="container mx-auto p-4">
      <div class="text-sm breadcrumbs">
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/api/products/">Products</a></li>
          <li>{{ product.name }}</li>
        </ul>
      </div>
      <div class="flex flex-col md:flex-row gap-6 mt-4">
        <div class="w-full md:w-1/2">
          <img src="{{ product.image|default:'https://via.placeholder.com/400' }}" alt="{{ product.name }}" class="w-full h-auto zoom-img" data-zoom-image="{{ product.image|default:'https://via.placeholder.com/800' }}">
        </div>
        <div class="w-full md:w-1/2">
          <h2 class="text-2xl font-semibold">{{ product.name }}</h2>
          <div class="text-yellow-500">★★★★☆ (4.5) <!-- Static for now --></div>
          <p class="text-3xl font-bold mt-2">${{ product.price|floatformat:2 }}</p>
          {% if product.discount > 0 %}
            <p class="text-green-600">Discount: {{ product.discount }}%</p>
          {% endif %}
          <p class="mt-2">Stock: {{ product.stock }} available</p>
          {% if product.stock <= 0 %}
            <p class="text-red-500">Out of Stock</p>
          {% endif %}
          <div class="mt-4">
            <h3 class="text-lg font-medium">Highlights</h3>
            <ul class="list-disc list-inside">
              <li>Weight: {{ product.weight|default:'N/A' }}g</li>
              <li>Size: {{ product.size|default:'N/A' }}</li>
            </ul>
          </div>
          <div class="mt-4">
            <h3 class="text-lg font-medium">Reviews</h3>
            <p>Review placeholder (to be implemented)</p>
          </div>
        </div>
      </div>
      <div class="mt-6">
        <h3 class="text-lg font-medium">Related Products</h3>
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
          <!-- Related products placeholder -->
        </div>
      </div>
    </div>
    <script>
      new ZoomVanilla('img.zoom-img', {
        onOpen: function() { this.target.classList.add('zoomed'); },
        onClose: function() { this.target.classList.remove('zoomed'); },
      });
    </script>
  </body>
  </html>